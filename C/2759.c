#include <stdio.h>
#include <stdlib.h>

int main()
{
    //PROJETO 2759 URI
  char A;
  char B;
  char C;
  scanf("%c %c %c", &A, &B, &C);


  printf("A = %c, B = %c, C = %c\n", A, B, C);
  printf("A = %c, B = %c, C = %c\n", B, C, A);
  printf("A = %c, B = %c, C = %c\n", C, A, B);
    return 0;
}
